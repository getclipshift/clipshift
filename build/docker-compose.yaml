version: '3.9'

services:
  builder:
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ..:/go/src/clipshift
    working_dir: /go/src/clipshift
    command: build -f build/goreleaser.yml --rm-dist --snapshot